# Exact versions, so every machine and the CI run build the same numbers.
# Needs Python 3.11 or newer.
duckdb==1.5.5
matplotlib==3.11.2
numpy==2.4.6
openpyxl==3.1.5
pandas==3.0.6
pyarrow==25.0.1
pyyaml==6.0.3
scikit-learn==1.9.1
scipy==1.17.1
