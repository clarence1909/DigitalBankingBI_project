# ER diagram

<!-- Generated by src/docs.py from the relationships in sql/schema.yml. Edit the source and rerun the pipeline, not this file. -->

The star schema in the `marts` schema: dimensions (`dim_`) describe who and what, and facts (`fct_`) record what happened. Each box shows the table's keys; every column is listed in the [data dictionary](04_data_dictionary.md). Lines run from the table that is referenced (one) to the table that references it (many), labelled with the joining column. All data is synthetic.

```mermaid
erDiagram
    dim_channel ||--o{ dim_customer : "acquisition_channel"
    fct_onboarding_funnel ||--o{ dim_customer : "applicant_id"
    dim_customer ||--o{ dim_account : "customer_id"
    dim_product ||--o{ dim_account : "product_code"
    dim_channel ||--o{ fct_onboarding_funnel : "acquisition_channel"
    dim_account ||--o{ fct_ledger_postings : "account_id"
    dim_customer ||--o{ fct_ledger_postings : "customer_id"
    dim_product ||--o{ fct_ledger_postings : "product_code"
    dim_date ||--o{ fct_ledger_postings : "posting_date"
    fct_card_authorisations ||--o{ fct_ledger_postings : "auth_id"
    dim_account ||--o{ fct_card_authorisations : "card_id"
    dim_customer ||--o{ fct_card_authorisations : "customer_id"
    dim_date ||--o{ fct_card_authorisations : "auth_date"
    dim_account ||--o{ fct_account_balances_monthly : "account_id"
    dim_customer ||--o{ fct_account_balances_monthly : "customer_id"
    dim_date ||--o{ fct_account_balances_monthly : "month_end_date"
    dim_customer ||--o{ fct_loan_status_monthly : "customer_id"
    dim_date ||--o{ fct_loan_status_monthly : "month_end_date"
    dim_channel ||--o{ fct_marketing_spend : "channel"
    dim_customer ||--o{ fct_customer_monthly : "customer_id"
    dim_date {
        date date_day PK
    }
    dim_channel {
        varchar channel PK
    }
    dim_product {
        varchar product_code PK
    }
    dim_customer {
        varchar customer_id PK
        varchar acquisition_channel FK
        varchar applicant_id FK
    }
    dim_account {
        varchar account_id PK
        varchar customer_id FK
        varchar product_code FK
    }
    fct_onboarding_funnel {
        varchar applicant_id PK
        varchar acquisition_channel FK
    }
    fct_ledger_postings {
        varchar posting_id PK
        varchar account_id FK
        varchar customer_id FK
        varchar product_code FK
        date posting_date FK
        varchar auth_id FK
    }
    fct_card_authorisations {
        varchar auth_id PK
        varchar card_id FK
        varchar customer_id FK
        date auth_date FK
    }
    fct_account_balances_monthly {
        varchar account_id PK, FK
        date month_end_date PK, FK
        varchar customer_id FK
    }
    fct_loan_status_monthly {
        varchar loan_id PK
        date month_end_date PK, FK
        varchar customer_id FK
    }
    fct_marketing_spend {
        date month_start PK
        varchar line_label PK
        varchar channel FK
    }
    fct_customer_monthly {
        varchar customer_id PK, FK
        date month_start PK
    }
```

## Table types

| Table | Type | Grain |
|---|---|---|
| `marts.dim_date` | Dimension | One row per day |
| `marts.dim_channel` | Dimension | One row per channel |
| `marts.dim_product` | Dimension | One row per product |
| `marts.dim_customer` | Dimension | One row per customer |
| `marts.dim_account` | Dimension | One row per account |
| `marts.fct_onboarding_funnel` | Fact: accumulating snapshot | One row per applicant |
| `marts.fct_ledger_postings` | Fact: transaction | One row per posting |
| `marts.fct_card_authorisations` | Fact: transaction | One row per authorisation |
| `marts.fct_account_balances_monthly` | Fact: periodic snapshot | One row per savings or fixed deposit account per month |
| `marts.fct_loan_status_monthly` | Fact: periodic snapshot | One row per loan per month on the books |
| `marts.fct_marketing_spend` | Fact: periodic (monthly spend by line) | One row per sheet line per month |
| `marts.fct_customer_monthly` | Fact: periodic snapshot | One row per customer per month while their account is open |
